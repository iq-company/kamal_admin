global:
  scrape_interval: 15s
scrape_configs:
  - job_name: 'nodes'
    static_configs:
      - targets:
{% for host in groups['all'] %}
{%   if 'monitoring_server' in hostvars[host].report_metrics_to.split(',') %}
        - {{ hostvars[host].ansible_host }}:9100
{%   endif %}
{% endfor %}
